#pragma once

#include "quantum.h"

// Defines the keycodes used by our macros in process_record_user
enum default_layouts {
    QWERTY = 0,
    AZERTY = 1,
    QWERTZ = 2,
    DVORAK = 3,
    COLEMAK = 4,
};
